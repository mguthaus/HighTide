// Auto-generated tc_sram replacement for ASAP7 synthesis.
// Maps large memories to fakeram macros; small memories stay as FFs.
// Generated by generate_sram_mapping.py from fakeram.cfg.
// DO NOT EDIT - regenerate by running setup.sh.

module tc_sram #(
  parameter int unsigned NumWords     = 32'd1024,
  parameter int unsigned DataWidth    = 32'd128,
  parameter int unsigned ByteWidth    = 32'd8,
  parameter int unsigned NumPorts     = 32'd2,
  parameter int unsigned Latency      = 32'd1,
  parameter              SimInit      = "none",
  parameter bit          PrintSimCfg  = 1'b0,
  parameter              ImplKey      = "none",
  // DEPENDENT PARAMETERS, DO NOT OVERWRITE!
  parameter int unsigned AddrWidth = (NumWords > 32'd1) ? $clog2(NumWords) : 32'd1,
  parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth,
  parameter type         addr_t    = logic [AddrWidth-1:0],
  parameter type         data_t    = logic [DataWidth-1:0],
  parameter type         be_t      = logic [BeWidth-1:0]
) (
  input  logic                 clk_i,
  input  logic                 rst_ni,
  input  logic  [NumPorts-1:0] req_i,
  input  logic  [NumPorts-1:0] we_i,
  input  addr_t [NumPorts-1:0] addr_i,
  input  data_t [NumPorts-1:0] wdata_i,
  input  be_t   [NumPorts-1:0] be_i,
  output data_t [NumPorts-1:0] rdata_o
);

  // ──────────────────────────────────────────────────────────────────
  // 256x256: direct → fakeram7_256x256
  // ──────────────────────────────────────────────────────────────────
  if (NumPorts == 1 && NumWords == 256 && DataWidth == 256) begin : gen_256x256

    fakeram7_256x256 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_i[0] ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rdata_o[0] )
    );

  // ──────────────────────────────────────────────────────────────────
  // 512x256: address-banked → 2x fakeram7_256x256
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 512 && DataWidth == 256) begin : gen_512x256

    logic bank_sel;
    logic bank_sel_q;
    logic [7:0] bank_addr;
    logic [255:0] rd0, rd1;
    
    assign bank_sel  = addr_i[0][8];
    assign bank_addr = addr_i[0][7:0];
    
    fakeram7_256x256 i_bank0 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & ~bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd0 )
    );
    
    fakeram7_256x256 i_bank1 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd1 )
    );
    
    always_ff @(posedge clk_i or negedge rst_ni) begin
      if (!rst_ni) bank_sel_q <= 1'b0;
      else if (req_i[0]) bank_sel_q <= bank_sel;
    end
    
    assign rdata_o[0] = bank_sel_q ? rd1 : rd0;

  // ──────────────────────────────────────────────────────────────────
  // Nx512: width-split → 2x fakeram7_256x256
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords <= 256 && DataWidth == 512) begin : gen_Nx512

    logic [7:0] padded_addr;
    assign padded_addr = {{(8-AddrWidth){1'b0}}, addr_i[0]};
    
    logic [255:0] rd_lo, rd_hi;
    
    fakeram7_256x256 i_lo (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][255:0] ),
      .rw0_rd_out  ( rd_lo )
    );
    
    fakeram7_256x256 i_hi (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][511:256] ),
      .rw0_rd_out  ( rd_hi )
    );
    
    assign rdata_o[0] = {rd_hi, rd_lo};

  // ──────────────────────────────────────────────────────────────────
  // 256x(<256): width-padded → fakeram7_256x256
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 256 && DataWidth < 256 && DataWidth > 128) begin : gen_256xpad256

    logic [7:0] addr_trunc;
    assign addr_trunc = addr_i[0][7:0];
    
    logic [255:0] rd_padded;
    logic [255:0] wd_padded;
    assign wd_padded = {{256-DataWidth{1'b0}}, wdata_i[0]};
    
    fakeram7_256x256 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_trunc ),
      .rw0_wd_in   ( wd_padded ),
      .rw0_rd_out  ( rd_padded )
    );
    
    assign rdata_o[0] = rd_padded[DataWidth-1:0];

  // ──────────────────────────────────────────────────────────────────
  // 64x512: direct → fakeram7_64x512
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 64 && DataWidth == 512) begin : gen_64x512

    fakeram7_64x512 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_i[0] ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rdata_o[0] )
    );

  // ──────────────────────────────────────────────────────────────────
  // 128x512: address-banked → 2x fakeram7_64x512
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 128 && DataWidth == 512) begin : gen_128x512

    logic bank_sel;
    logic bank_sel_q;
    logic [5:0] bank_addr;
    logic [511:0] rd0, rd1;
    
    assign bank_sel  = addr_i[0][6];
    assign bank_addr = addr_i[0][5:0];
    
    fakeram7_64x512 i_bank0 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & ~bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd0 )
    );
    
    fakeram7_64x512 i_bank1 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd1 )
    );
    
    always_ff @(posedge clk_i or negedge rst_ni) begin
      if (!rst_ni) bank_sel_q <= 1'b0;
      else if (req_i[0]) bank_sel_q <= bank_sel;
    end
    
    assign rdata_o[0] = bank_sel_q ? rd1 : rd0;

  // ──────────────────────────────────────────────────────────────────
  // Nx1024: width-split → 2x fakeram7_64x512
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords <= 64 && DataWidth == 1024) begin : gen_Nx1024

    logic [5:0] padded_addr;
    assign padded_addr = {{(6-AddrWidth){1'b0}}, addr_i[0]};
    
    logic [511:0] rd_lo, rd_hi;
    
    fakeram7_64x512 i_lo (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][511:0] ),
      .rw0_rd_out  ( rd_lo )
    );
    
    fakeram7_64x512 i_hi (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][1023:512] ),
      .rw0_rd_out  ( rd_hi )
    );
    
    assign rdata_o[0] = {rd_hi, rd_lo};

  // ──────────────────────────────────────────────────────────────────
  // 64x(<512): width-padded → fakeram7_64x512
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 64 && DataWidth < 512 && DataWidth > 256) begin : gen_64xpad512

    logic [5:0] addr_trunc;
    assign addr_trunc = addr_i[0][5:0];
    
    logic [511:0] rd_padded;
    logic [511:0] wd_padded;
    assign wd_padded = {{512-DataWidth{1'b0}}, wdata_i[0]};
    
    fakeram7_64x512 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_trunc ),
      .rw0_wd_in   ( wd_padded ),
      .rw0_rd_out  ( rd_padded )
    );
    
    assign rdata_o[0] = rd_padded[DataWidth-1:0];

  // ──────────────────────────────────────────────────────────────────
  // 256x64: direct → fakeram7_256x64
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 256 && DataWidth == 64) begin : gen_256x64

    fakeram7_256x64 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_i[0] ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rdata_o[0] )
    );

  // ──────────────────────────────────────────────────────────────────
  // 512x64: address-banked → 2x fakeram7_256x64
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 512 && DataWidth == 64) begin : gen_512x64

    logic bank_sel;
    logic bank_sel_q;
    logic [7:0] bank_addr;
    logic [63:0] rd0, rd1;
    
    assign bank_sel  = addr_i[0][8];
    assign bank_addr = addr_i[0][7:0];
    
    fakeram7_256x64 i_bank0 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & ~bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd0 )
    );
    
    fakeram7_256x64 i_bank1 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd1 )
    );
    
    always_ff @(posedge clk_i or negedge rst_ni) begin
      if (!rst_ni) bank_sel_q <= 1'b0;
      else if (req_i[0]) bank_sel_q <= bank_sel;
    end
    
    assign rdata_o[0] = bank_sel_q ? rd1 : rd0;

  // ──────────────────────────────────────────────────────────────────
  // Nx128: width-split → 2x fakeram7_256x64
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords <= 256 && DataWidth == 128) begin : gen_Nx128

    logic [7:0] padded_addr;
    assign padded_addr = {{(8-AddrWidth){1'b0}}, addr_i[0]};
    
    logic [63:0] rd_lo, rd_hi;
    
    fakeram7_256x64 i_lo (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][63:0] ),
      .rw0_rd_out  ( rd_lo )
    );
    
    fakeram7_256x64 i_hi (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][127:64] ),
      .rw0_rd_out  ( rd_hi )
    );
    
    assign rdata_o[0] = {rd_hi, rd_lo};

  // ──────────────────────────────────────────────────────────────────
  // 256x(<64): width-padded → fakeram7_256x64
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 256 && DataWidth < 64 && DataWidth > 32) begin : gen_256xpad64

    logic [7:0] addr_trunc;
    assign addr_trunc = addr_i[0][7:0];
    
    logic [63:0] rd_padded;
    logic [63:0] wd_padded;
    assign wd_padded = {{64-DataWidth{1'b0}}, wdata_i[0]};
    
    fakeram7_256x64 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_trunc ),
      .rw0_wd_in   ( wd_padded ),
      .rw0_rd_out  ( rd_padded )
    );
    
    assign rdata_o[0] = rd_padded[DataWidth-1:0];

  // ──────────────────────────────────────────────────────────────────
  // 128x48: direct → fakeram7_128x48
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 128 && DataWidth == 48) begin : gen_128x48

    fakeram7_128x48 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_i[0] ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rdata_o[0] )
    );

  // ──────────────────────────────────────────────────────────────────
  // 256x48: address-banked → 2x fakeram7_128x48
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 256 && DataWidth == 48) begin : gen_256x48

    logic bank_sel;
    logic bank_sel_q;
    logic [6:0] bank_addr;
    logic [47:0] rd0, rd1;
    
    assign bank_sel  = addr_i[0][7];
    assign bank_addr = addr_i[0][6:0];
    
    fakeram7_128x48 i_bank0 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & ~bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd0 )
    );
    
    fakeram7_128x48 i_bank1 (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] & bank_sel ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( bank_addr ),
      .rw0_wd_in   ( wdata_i[0] ),
      .rw0_rd_out  ( rd1 )
    );
    
    always_ff @(posedge clk_i or negedge rst_ni) begin
      if (!rst_ni) bank_sel_q <= 1'b0;
      else if (req_i[0]) bank_sel_q <= bank_sel;
    end
    
    assign rdata_o[0] = bank_sel_q ? rd1 : rd0;

  // ──────────────────────────────────────────────────────────────────
  // Nx96: width-split → 2x fakeram7_128x48
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords <= 128 && DataWidth == 96) begin : gen_Nx96

    logic [6:0] padded_addr;
    assign padded_addr = {{(7-AddrWidth){1'b0}}, addr_i[0]};
    
    logic [47:0] rd_lo, rd_hi;
    
    fakeram7_128x48 i_lo (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][47:0] ),
      .rw0_rd_out  ( rd_lo )
    );
    
    fakeram7_128x48 i_hi (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( padded_addr ),
      .rw0_wd_in   ( wdata_i[0][95:48] ),
      .rw0_rd_out  ( rd_hi )
    );
    
    assign rdata_o[0] = {rd_hi, rd_lo};

  // ──────────────────────────────────────────────────────────────────
  // 128x(<48): width-padded → fakeram7_128x48
  // ──────────────────────────────────────────────────────────────────
  end else if (NumPorts == 1 && NumWords == 128 && DataWidth < 48 && DataWidth > 24) begin : gen_128xpad48

    logic [6:0] addr_trunc;
    assign addr_trunc = addr_i[0][6:0];
    
    logic [47:0] rd_padded;
    logic [47:0] wd_padded;
    assign wd_padded = {{48-DataWidth{1'b0}}, wdata_i[0]};
    
    fakeram7_128x48 i_mem (
      .rw0_clk     ( clk_i ),
      .rw0_ce_in   ( req_i[0] ),
      .rw0_we_in   ( we_i[0] ),
      .rw0_addr_in ( addr_trunc ),
      .rw0_wd_in   ( wd_padded ),
      .rw0_rd_out  ( rd_padded )
    );
    
    assign rdata_o[0] = rd_padded[DataWidth-1:0];

  // ──────────────────────────────────────────────────────────────────
  // All other sizes: behavioral model (synthesized to FFs)
  // ──────────────────────────────────────────────────────────────────
  end else begin : gen_ff

    data_t sram [NumWords-1:0];
    addr_t [NumPorts-1:0] r_addr_q;

    data_t [NumPorts-1:0][Latency-1:0] rdata_q, rdata_d;
    if (Latency == 32'd0) begin : gen_no_read_lat
      for (genvar i = 0; i < NumPorts; i++) begin : gen_port
        assign rdata_o[i] = (req_i[i] && !we_i[i]) ? sram[addr_i[i]] : sram[r_addr_q[i]];
      end
    end else begin : gen_read_lat
      always_comb begin
        for (int unsigned i = 0; i < NumPorts; i++) begin
          rdata_o[i] = rdata_q[i][0];
          for (int unsigned j = 0; j < (Latency-1); j++)
            rdata_d[i][j] = rdata_q[i][j+1];
          rdata_d[i][Latency-1] = (req_i[i] && !we_i[i]) ? sram[addr_i[i]] : sram[r_addr_q[i]];
        end
      end
    end

    always_ff @(posedge clk_i or negedge rst_ni) begin
      if (!rst_ni) begin
        for (int i = 0; i < NumPorts; i++)
          r_addr_q[i] <= {AddrWidth{1'b0}};
      end else begin
        for (int unsigned i = 0; i < NumPorts; i++) begin
          if (Latency != 0) begin
            for (int unsigned j = 0; j < Latency; j++)
              rdata_q[i][j] <= rdata_d[i][j];
          end
        end
        for (int unsigned i = 0; i < NumPorts; i++) begin
          if (req_i[i]) begin
            if (we_i[i]) begin
              for (int unsigned j = 0; j < BeWidth; j++) begin
                if (be_i[i][j])
                  sram[addr_i[i]][j*ByteWidth+:ByteWidth] <= wdata_i[i][j*ByteWidth+:ByteWidth];
              end
            end else begin
              r_addr_q[i] <= addr_i[i];
            end
          end
        end
      end
    end

  end

endmodule
